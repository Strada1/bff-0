const { Movie } = require('./movie');
const { Category } = require('./category');
const { Comment } = require('./comment');
const { Director } = require('./director');

module.exports = { Movie, Category, Comment, Director };
